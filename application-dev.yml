eureka:
  client:
    service-url:
      #服务注册中心的配置内容，指定服务注册中心的Url
      defaultZone: http://server1:20001/eureka/
#Jdbc 配置信息
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dev?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    userName: dev
    passWord: dev123456
